#TODO: Es dev dado que el volumen se enlaza al directorio local

services:
  flaskapp:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: flask_con_volumen
    ports:
      - "8080:8080"
    environment:
      VAR2: Mundo variable
    working_dir: /dagster_home        # ✅ Contenedor se posiciona aquí
    command: python prueba.py         # ✅ Ejecuta el archivo